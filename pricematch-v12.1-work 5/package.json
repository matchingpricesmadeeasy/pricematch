{
  "name": "pricematch-engine",
  "private": true,
  "version": "12.2.0",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio",
    "alerts:check": "curl -X POST -H \"Authorization: Bearer $CRON_SECRET\" http://localhost:3000/api/alerts/check",
    "db:push": "prisma db push",
    "typecheck": "tsc --noEmit",
    "lint:check": "tsc --noEmit",
    "test:qa": "node scripts/qa-smoke.mjs",
    "db:migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "next": "^15.5.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "@prisma/client": "^6.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.8.0",
    "prisma": "^6.16.0"
  }
}
